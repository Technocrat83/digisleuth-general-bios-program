# URPH_P1_RF_CLASS2_INSTRUMENT_HARNESS_v0.1

Executable materialization of frozen Git contract `URPH_P1_RF_CLASS2_WITNESS_MATERIALIZATION_CONTRACT_v0.1`.

Jurisdiction: `INSTRUMENT_IMPLEMENTATION_AND_FIXTURE_EXECUTION_ONLY`  
Standing: `PRE_BASELINE • PRE_APPLICABILITY • PRE_INTERVENTION`

`Instrument Execution != Baseline Evidence != Applicability != Pressure Evidence`

Run:
```bash
PYTHONPATH=src python -m unittest discover -s tests -v
PYTHONPATH=src python -m urph_harness.cli conform --out artifacts
```

Baseline materialization is eligible only at `M_I=[1,1,1,1,1,1,1,1,1]` with `PASS_I=true`. Eligibility confers zero scientific standing on URPH, CUC, Computational Physiology, Track E, Canon, or PP.
