# BIOS Phenotype Observation Conformance Runner v0.1

## Standing

`MATERIALIZED_EXECUTION_BLOCKED_BY_MISSING_BOUND_BYTES`

This runner is deliberately constitutionally stupid. It has fixture execution and result-capture authority only.

**Runner Execution Authority != Schema Authority != Interpretation Authority.**

The runner does not define or repair the frozen observation contract, infer intended reasons, remap detector jurisdiction, generate phenotype features, access H_SIGMA or y_phi, instantiate Phi_Gamma, or make scientific claims.

## Frozen bindings required before execution

1. Exact 24-fixture manifest: OC-01..OC-12 and PC-01..PC-12.
2. Exact H_GAMMA bytes whose SHA-256 equals the manifest binding.
3. Executable `BIOS_PHENOTYPE_BLIND_OBSERVATION_INTERFACE_v0.1` module exposing `invoke_interface(...)`.

The exact frozen matrix is authoritative for all detector assignments, including OC-06, OC-07, OC-09, OC-10, and OC-11. The runner contains no provisional replacements.

## Noncompensatory adjudication

The aggregate vector is:

`R_OC = <P_plus, P_minus, P_J, P_D, P_GAMMA, P_B>`

Only `<1,1,1,1,1,1>` emits `BATTERY_PASS`. There is no percentage score, weighting, near-pass, reason-code remapping, or rejection-only shortcut.

`H_GAMMA_before == H_GAMMA_after` is executable. Mutation of Gamma forces apparatus-integrity failure regardless of chamber performance.

## Non-retry law

A failed fixture is recorded exactly. Independent fixtures may continue if execution remains valid. The runner never patches the interface or fixture and retries within the same run identity.

## Current epistemic state

- Execution: NOT PERFORMED
- Real BIOS specimens: STRICTLY PROHIBITED
- H_SIGMA1 delta: 0
- Phenotype evidence: 0
- PP: BLOCKED

The observer-test machine exists; the phenotype experiment has not begun.
