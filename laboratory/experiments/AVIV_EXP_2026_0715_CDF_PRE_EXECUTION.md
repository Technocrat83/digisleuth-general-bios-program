# AVIV-EXP-2026-0715-CDF

**Class:** COMPUTATIONAL_FALSIFICATION_EXPERIMENT  
**Posture:** FROZEN_PRE_EXECUTION  
**Scientific Standing:** UNESTABLISHED  
**Canon Delta:** 0  
**PP:** BLOCKED

## Question

Can dynamic governance dressing improve federation coherence over an equally lawful static constitutional membrane without purchasing that improvement through jurisdictional leakage, authority expansion, or transport corruption?

## Treatment regimes

- `F_U`: raw coupling reference; non-theorem-bearing.
- `F_C`: static constitutionally admissible control.
- `F_G`: governance-dressed dynamic coupling.

## Decoherence vector

\[
\mathbf D=\langle D_s,D_p,D_j,D_x\rangle
\]

for semantic, provenance, jurisdictional, and state decoherence.

Derived scalar:

\[
\Gamma=1-\tfrac14(D_s+D_p+D_j+D_x)
\]

Execution fixtures may emit only `D_raw`, required witnesses, and trace residue. Gamma, contrasts, and verdicts belong to the adjudication chamber.

## Candidate decision rule

`F_G` outranks `F_C` iff positive dynamic coherence advantage coexists with all non-compensable gates:

- jurisdiction: `D_j,G <= D_j,C` and `D_j,G < epsilon_j`
- authority monotonicity: `Authority(E_active) subseteq Authority(E_constitutional)`
- transport: declared `I_T` invariants preserved

Coherence gain may not purchase authority expansion.

## Experimental matrix

3 treatments x 5 stress regimes = 15 cells:

1. nominal baseline
2. semantic perturbation
3. provenance severing
4. jurisdictional injection
5. execution saturation

The theorem-bearing comparison is `F_G` vs `F_C` within each matched row. Aggregate advantage does not erase row-specific failure.

## Chamber separation

Execution Chamber != Adjudication Chamber.

Measurement != Interpretation != Standing Assignment.

Epistemic lifecycle:

`UNOBSERVED -> EXECUTED -> INVALID | ADMISSIBLE -> FAIL | PASS`

`INVALID != FAIL`; invalid apparatus residue has zero hypothesis weight.

## Required execution hygiene

Each cell is precommitted and begins from a canonical reset state witness `W_S0`. Required residue:

`R_t,r = { W_S0, W_match, W_I_T, W_K, D_raw, T_trace }`

`W_K` is not applicable only for `F_U`. For `F_C` and `F_G`, missing Stage-9 admission evidence invalidates the cell.

Matched Inputs + Unmatched Initial Runtime State != Matched Experiment.

No theorem evaluation, PP, Canon mutation, or physiological interpretation belongs in the execution harness.
