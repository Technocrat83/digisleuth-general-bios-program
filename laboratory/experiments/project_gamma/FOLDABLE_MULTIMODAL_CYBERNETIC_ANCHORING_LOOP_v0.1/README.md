# FMCAL Headless Conformance Harness v0.1

This synthetic harness falsifies ambiguous positive confirmation in the proposed `FOLDABLE_MULTIMODAL_CYBERNETIC_ANCHORING_LOOP_v0.1`. It performs no physical-device I/O and grants no scientific, physiological, constitutional, production, or PP standing.

## Frozen boundary

```text
sensor evidence -> typed estimate -> invariant lens decision -> haptic command -> authenticated delivery receipt
```

The stages are not interchangeable:

```text
haptic command != actuator delivery != user perception != semantic comprehension
```

Only an authenticated receipt bound to the exact issued command, sensor event, pattern, delivery state, and fresh nonce may establish `CONFIRMED_DELIVERED`. Even then, the result is only an actuator-delivery witness.

`FIRM_INTERRUPTION` is a typed refusal signal, not a positive confirmation. `ZERO_PATTERN` is the mandatory result when there is no certified transition. The abstract waveform identifiers are capability requirements, not claims that arbitrary foldable hardware can reproduce the proposed physical waveforms.

## Run

```bash
python -m unittest discover -s tests -v
```

## Status

- Architecture: `INSTANTIATED_CANDIDATE`
- Harness: `SYNTHETIC_HEADLESS`
- Device execution: `BLOCKED`
- Scientific standing: `UNEVALUATED_BEYOND_SYNTHETIC_HARNESS`
- Physiological standing: `ZERO`
- Constitutional standing: `ZERO`
- PP: `HARD_BLOCKED`
