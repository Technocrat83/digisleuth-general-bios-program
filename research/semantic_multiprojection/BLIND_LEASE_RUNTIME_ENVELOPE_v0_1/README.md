# Blind Lease Runtime Envelope v0.1

**Standing:** MATERIALIZED_UNQUALIFIED

BLE is an execution-authorization membrane around the immutable `MP_BATTERY_v0_1`.
It does not modify the battery, issue a lease, authorize dispatch, execute MP0–MP6,
or generate experimental observations.

## Governing separations

- Materialized apparatus != executable apparatus != qualified lease != issued lease != dispatch.
- Blindness ideal != blindness implementation predicate.
- Declared separation != demonstrated separation.
- BLE mechanical execution != MP experimental execution.
- Execution qualification != semantic conformance != scientific validation.

## Mechanical scope

BLE may verify byte identity, materialization, referential closure, blindness controls,
evaluator isolation, nonce/expiry/replay semantics, authority partitioning, and terminal
state closure. Tests use synthetic fixtures only.

## Historical battery boundary

The configured battery is read-only. Qualification may compute a deterministic battery-tree
digest before and after inspection and requires equality as a mechanical witness of
`Delta MP_BATTERY = 0`.

## Terminal posture

Materialization ends at `BLE_MATERIALIZED_UNQUALIFIED`. `L1` does not exist and no
qualification vector is adjudicated by materialization itself.
