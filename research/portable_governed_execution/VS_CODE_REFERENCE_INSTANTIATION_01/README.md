# Portable Governed Execution — VS Code Reference Instantiation 01

Status: REFERENCE_IMPLEMENTATION_MATERIALIZATION_ONLY

This folder is a bounded VS Code habitat for the frozen Portable Governed Execution architecture target.

It does not establish executor conformance, portability, editor-wide containment, validation, acceptance, promotion, or Canon standing.

## Governing laws

- Execution may produce residue; only admissible authority may change standing.
- Embodiment may constrain authority realization; it may never enlarge the authenticated grant.

## Primary objects represented

- immutable execution contract payload
- revision-bound attempt receipt
- submitted transition-witness ledger surface
- deterministic standing reconstruction stub

## First hosted attempt

Open this folder directly in VS Code, then run:

`Tasks: Run Task -> PGE: bounded reference attempt`

The task executes exactly one bounded reference attempt and writes a receipt under `receipts/`.

A completed receipt is host-specific execution evidence only. It does not independently establish conformance or promotion standing.

## Evidence boundary

Current intended progression:

`reference implementation materialized -> VS Code-hosted attempt -> later single-executor conformance`

Roadmap is not authorization and is not an automatic standing transition.
