# ORIENTATION_TASK_REGIME_AND_SOURCE_REFERENCE_CONTRACT_v0.1

```yaml
APPARATUS_REGISTRATION_MANIFEST:
  apparatus_id: ORIENTATION_TASK_REGIME_AND_SOURCE_REFERENCE_CONTRACT_v0.1
  cone: MINIMAL_COMPUTATIONAL_CARTOGRAPHY_SUBSTRATE_v0.1
  posture: SEALED_SPECIFICATION_PRE_ABLATION
  standing: RATIFIED_ARCHITECTURAL_CANDIDATE
  ordering_enforcement:
    contract_precedence: "TASK_REGIME_AND_SOURCE_REF_CONTRACT < ABLATION_MATERIALIZATION"
  persistence_latches:
    Theta_Git: SATISFIED_AS_SPECIFICATION_ONLY
    Theta_Obsidian: PREMATURE_WRITE_BLOCKED
    Theta_Airtable: UNMET
    Theta_Sheets: UNMET
    Theta_Execution: UNMET
  standing_deltas:
    Delta_Materialization: 0
    Delta_Execution: 0
    Delta_Observation: 0
    Delta_Evidence: 0
    Delta_Authority: 0
    Delta_Canon: 0
    Delta_PP: 0
  pp_gate: BLOCKED
```

## Scientific object

The transformation boundary is representation-independent:

`Pi_C : L_computational -> M_cartographic`

The governing separation is:

`Information Preservation != Orientation Sufficiency != Orientation != Traversal Authority`.

Candidate information basis:

`C_0 = <I, P, R, J, S, T, X>`

Minimality is UNEARNED. The experiment must discover any regime-relative minimal constitutive subset rather than install one by nomenclature.

## Separated epistemic roles

1. Source Resolver `R_L`: derives source reference `Y_L` directly from admitted lineage without cartographic projection.
2. Projection Operator `Pi_C`: maps lineage `L` into cartographic morphology `M`.
3. Helm `H`: consumes `M` exclusively and reports parsed situational coordinates `Y_hat_H`.
4. Orientation Adjudicator `A_O`: evaluates `Y_hat_H` against `Y_L` under frozen task regime `T_O`.

`Source Resolver != Projection Operator != Helm != Orientation Adjudicator`.

Projection Authority != Orientation Authority != Orientation Adjudication Authority.

## Source reference and anti-invention symmetry

`Y_L = <I*, A*, J*, T*, R*, M*>`

Each source coordinate possesses independent standing.

`Reference Availability != Reference Resolution != Reference Authority`.

Neither source extraction nor cartographic projection may manufacture distinctions absent from lineage:

`Distinction(Y_L) subseteq Distinction(L)`

`Distinction(M) subseteq Distinction(L)`

Therefore:

`D(Y_L) UNION D(M) SUBSETEQ D(L)`.

Reference fidelity and projection fidelity remain separate despite their common source.

## Frozen orientation task regime

`T_O = <D_I, D_A, D_J, D_T, D_R, D_M, rho>`

The distinction sets and resolution requirements are frozen ex ante. Observed Helm output has zero authority to mutate the task regime.

Coordinates:

- I / Identity: distinguish Self, Peer, Sub-organ, Alien, Disjoint; minimum resolution is unaliased RID match.
- A / Ancestry: distinguish Parent, Ancestor, Descendant, Fork, Supersession, Disconnected; minimum resolution is one-hop parentage plus root path.
- J / Jurisdiction: distinguish Inside, Outside, Boundary, Overlap, NonSubstitutive; minimum resolution is authority ceiling token J_x.
- T / Temporal: distinguish Predecessor, Concurrent, Successor, HistoricalEpoch, ActiveTip; minimum resolution is topological temporal localization.
- R / Relation: distinguish Constrains, Witnesses, Projects, Ingests, Quarantines; minimum resolution is typed edge category.
- M / Maneuver: recognize source-supported classification as A_plus, A_minus, or A_U. This is orientation only and confers no present execution eligibility.

## Four-valued adjudication algebra

For each coordinate k:

`A_k in {CORRECT_RESOLUTION, INCORRECT_RESOLUTION, JUSTIFIED_ABSTENTION, UNADJUDICABLE}`.

- CORRECT_RESOLUTION: Helm output conforms to source reference within the frozen distinction set and resolution requirement.
- INCORRECT_RESOLUTION: Helm output contradicts source reference, confuses active boundaries, or asserts unauthenticated linkage.
- JUSTIFIED_ABSTENTION: Helm reports unresolved because the source itself cannot lawfully resolve the required distinction.
- UNADJUDICABLE: source reference lacks standing, its witness is invalid, or the evaluation regime cannot adjudicate the claim.

The full raw observable remains uncollapsed:

`A_H = <A_I, A_A, A_J, A_T, A_R, A_M>`.

The aggregate `phi_O` is safety/competence summary only and is prohibited from substituting for `A_H` during ablation adjudication.

`CORRECT_RESOLUTION != JUSTIFIED_ABSTENTION` even when both are epistemically safe.

## Correctness and resolution axes

`Q_O = Epistemic Correctness`

`R_O = Recoverable Orientation Resolution`

`Q_O _|_ R_O`.

`CORRECT -> ABSTENTION` means resolution loss with epistemic safety preserved.

`CORRECT -> INCORRECT` means orientation corruption.

`CORRECT -> UNADJUDICABLE` means reference/adjudication failure and does not establish projection defect.

`R_O` is DEFINED_CONCEPTUALLY_UNOPERATIONALIZED. No executable numerical aggregation semantics are admitted yet.

`Resolution Concept Defined != Resolution Metric Operationalized`.

## Maneuver coordinate fence

`M* = <A_plus, A_minus, A_U>`.

- A_plus: source establishes the candidate maneuver as admissible within the represented state.
- A_minus: source establishes intersection with an active prohibition/exclusion boundary.
- A_U: source establishes neither positive admissibility nor prohibition.

`a notin A_plus -/-> a in A_minus`.

`NOT_ESTABLISHED_AVAILABLE != ESTABLISHED_UNAVAILABLE`.

Correct maneuver orientation does not confer present execution authority:

`Y_hat_M = A_plus -/-> J_EX = 1`.

## Representation independence

Removal and replacement answer different questions:

`Remove(k)` tests constitutive contribution under the tested regime.

`Replace(k, phi(k))` tests representational equivalence and whether an orientation-bearing invariant survives carrier substitution.

`Coordinate Contribution != Encoding Necessity != Underlying Orientation Invariant`.

Literal spatial coordinates `X_literal` and relative geometry `G_relative` remain experimentally separable. Spatial adjacency is not computational relation unless explicitly typed as such.

## Causal attribution fence

Ablation must preserve typed transition semantics rather than collapse outcomes into a scalar.

- `C -> A`: resolution loss; representational deficiency may be present while epistemic safety survives.
- `C -> I`: orientation corruption.
- `C -> U`: reference/adjudication failure; neither validates nor falsifies the projection.

## Representation-change hypothesis

A computational cartography is adequate to the extent that it preserves task-relevant orientation information across representation change.

`Orientation-Preserving Projection != Authority-Preserving Projection`.

Prior constitutional occupation may provide orientation for discovering candidate future maneuver space without transmitting the authority of the prior occupation.

## Precedence and hold

`ORIENTATION_TASK_REGIME_AND_SOURCE_REFERENCE_CONTRACT_v0.1 < ABLATION_MATERIALIZATION`.

Ablation materialization remains blocked pending separate explicit authorization.

```yaml
HOLD_REGISTRATION_LEDGER:
  chamber_id: MINIMAL_COMPUTATIONAL_CARTOGRAPHY_SUBSTRATE_v0.1
  posture: SILENT_PRE_ABLATION_HOLD
  standing: SEALED_SPECIFICATION_PRE_ABLATION
  resolution_metric_status: DEFINED_CONCEPTUALLY_UNOPERATIONALIZED
  persisted_specification_spine: GIT_BOUND_IMMUTABLE
  organ_thresholds:
    Git: SATISFIED_AS_SPECIFICATION_ONLY
    Obsidian: PREMATURE_WRITE_BLOCKED
    Airtable: UNMET
    Sheets: UNMET
    Execution: UNMET
  deltas:
    Delta_Materialization: 0
    Delta_Execution: 0
    Delta_Observation: 0
    Delta_Evidence: 0
    Delta_Authority: 0
    Delta_Canon: 0
    Delta_PP: 0
  pp_gate: BLOCKED
```

No ablation fixtures, execution, observations, evidence, authority, Canon mutation, PP, Obsidian persistence, Airtable persistence, or Sheets persistence are authorized by this Git write.
